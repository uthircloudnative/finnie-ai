---
description: # /resume
---

Read AGENTS.md, docs/PROGRESS_LOG.md, docs/PROJECT_PLAN.md, and docs/CODE_REVIEW.md.
Check recent git commits (`git log -n 3 --oneline`) and run backend tests (`uv run python -m unittest discover -s tests`) to verify workspace health.
Summarize the current state of the app, recent accomplishments, and present the next prioritized tasks/options to the user for confirmation to start.